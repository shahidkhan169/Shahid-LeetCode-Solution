import java.util.*;
import java.io.*;
class Solution {
public double myPow(double x, int n) {
        double p;
        if(n>0)
        {
            return p=Math.pow(x,n);
        }
        else
        {
            return p=Math.pow(x,n);
        }
    
    }
};
